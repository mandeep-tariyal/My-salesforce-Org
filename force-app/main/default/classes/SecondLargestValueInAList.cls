public without sharing class SecondLargestValueInList{
    public static void findSecondLargest(){
        list<integer> Array = new List<Integer>{3,5,21,44,9,8};
        String returnValue = MergeSort.driverMethod(Array,Array.size()-1);
    }
}